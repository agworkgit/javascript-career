import testmodule # tells the compiler to access code from the testmodule file

testmodule.mult(10, 5) # to access a function we need to tell the compiler where to find the function
testmodule.div(50, 2)