flips = [
    'heads',
    'tails',
    'tails',
    'heads',
    'tails',
]

print(flips.count('heads')) # the method counts how many times 'heads' occurrs in the list (array)
print(flips.pop()) # 'returns' the last element in the list

# array instances inherit from the array class, which has methods such as .count() built-in